{
  "name": "data-mining-webapp",
  "version": "1.0.0",
  "description": "data-mining-project",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && server -d dist && cd client && npm install && npm run build",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^6.8.1",
    "morgan": "^1.10.0",
    "netlify-lambda": "^2.0.15",
    "nodemon": "^2.0.20"
  }
}